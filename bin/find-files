#!/usr/bin/env bash

given_file="$1"
selected=`ag -g "" | fzf --preview="cat {}" --preview-window=right:70%:wrap
--query="$given_file"`

if [ -z ${selected} ]; then
	echo "Please select an option"
else
	nvim $selected
fi

