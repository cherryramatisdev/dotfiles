#!/usr/bin/env bash

current_language=`setxkbmap -query | grep 'layout' | sed 's/layout: //' | sed 's/^ *//g'`

if [ $current_language = 'us' ]; then
    setxkbmap -layout br
    notify-send "Language switched to br"
else
    setxkbmap -layout us
    notify-send "Language switched to us"
fi
