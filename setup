#!/bin/sh

echo "Linking stuff..."

ln -sf "$PWD/.bashrc" "$HOME/.bashrc"
ln -sf "$PWD/scripts" "$HOME/bin"
ln -sf "$PWD/.gitconfig" "$HOME/.gitconfig"

mkdir -p "$HOME/.local/bin" || true

for i in vim lynx tmux git; do
	echo $i
	cd $i && sh ./setup
	cd -
done

echo "Finished ! :D"
